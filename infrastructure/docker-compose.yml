services:
  nats:
    # Pinned to the broker version the test suite runs (.nats-version), so
    # the dev broker and the per-test broker agree; bump both together.
    image: nats:2.14.3
    ports:
      # Bound to loopback: the dev broker is reachable only from this
      # host (assessment critique #4). Client connections require the
      # token from infrastructure/nats/nats.conf.
      - "127.0.0.1:4222:4222"
      - "127.0.0.1:8222:8222"
    command: ["--config", "/etc/nats/nats.conf"]
    volumes:
      - ./nats/nats.conf:/etc/nats/nats.conf:ro
      - nats-data:/data/nats

volumes:
  nats-data:
