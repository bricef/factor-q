factor-q status

Config
  NATS URL:         <NATS_URL>
  agents dir:       <CACHE_DIR>/agents
  cache dir:        <CACHE_DIR>

Daemon
  connection:       ✗ no daemon answered at <EDGE_ADDR>
  reason:           could not reach the edge at <EDGE_ADDR>: Connection refused (os error #)
  -> that is the finding: there is no runtime here to report on.
  -> stream health, the agent registry, projection rows and recovery state
     are the daemon's to answer, so they are absent below rather than stale.

Stores
  worker db:        <CACHE_DIR>/worker.db
  control-plane db: <CACHE_DIR>/control-plane.db
  projection db:    <CACHE_DIR>/projection.db
  state:            initialised — all three store files exist
