//! Adapter that implements [`LlmClient`](super::LlmClient) on top of the
//! `genai` crate.
//!
//! Placeholder — implementation lands in a follow-up step once the
//! executor is in place and can exercise it.

// Intentionally empty for now. The adapter will live here.
